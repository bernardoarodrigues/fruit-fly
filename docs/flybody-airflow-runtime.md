# Optional FlyBody airflow observation

The optional body integration passes its fixed two-second full-neural regression. The isolated reader [module](../fruitfly/flybody_airflow.py) also reproduces all four independently reviewed native geometry probes without advancing physics. Its [frozen check](../validation/flybody-airflow-module/plan.json) and [result](../validation/flybody-airflow-module/result.json) retain the sampled vectors, exact before/after bytes for nine named native arrays, and original actor-observation bytes. This is not an inventory of every MuJoCo state field.

The implementation samples the same proximal antenna origins used for odor. Point velocities come from native Jacobians, with source cm/s converted to mm/s. A neutral thorax-aligned basis follows the articulated head. [The geometry review](flybody-airflow-independent-review.md) establishes these frames and units while preserving the distinction between a proximal pivot and a distal arista or sensillum.

The integration uses an explicit `enable_wind` option, disabled by default. The native worker exposes point position, point velocity and head rotation computed on its existing detached diagnostic state. The host subtracts sensor velocity from configured ambient airflow and uses the existing tested `local_airflow` function. This supplies a sensory observation only: it introduces no force, deflection, release model, spike input or steering rule. The existing measured female antenna reference remains unsupported in this body backend.

All three frozen longer neural attempts concluded before changing their runtime dependencies. The [new plan](../validation/flybody-airflow-runtime/plan.json), SHA-256 `1c5f1b6946f993fe880ce14a37ca46ffa78d0be51da87cf3e6ff148347db3e7d`, freezes the integration at `6a1577b`. Its [result](../validation/flybody-airflow-runtime/result.json) compares the original locomotor-feedback prefix for all declared physical fields, ordered neural drives, RNG and spike hashes with airflow enabled. All 1,000 intervals match exactly. Every ordered spike and the actual final checkpoint are retained using the earlier lossless recorder.

Checks cover 1,001 distinct native ticks, including initialization. New origin positions equal existing odor sample points exactly. Independent component-wise host reconstruction differs by at most 3.56e−15 mm/s in relative velocity and 2.85e−14 degrees in source azimuth. The rotation, local vector finiteness, native clocks, reset/render/zero-step controls and resource checks pass. This is a two-second integration regression; the earlier twelve-second records used airflow disabled.

The [live-viewer inspection](../validation/flybody-airflow-viewer/inspection.json) records a run to 5.44 s with changing actual point velocities and head-frame flow. The camera change preserves cached wind, physical, neural and resource telemetry; reset restores the initial values exactly. The initial stationary antennae report approximately 2 mm/s ambient flow from the front. The display labels the sampling point and missing neural/receptor mapping. Launch `configs/male-flybody-rolling-airflow-probe.json` on port 8773, then use Resume. The default configuration keeps airflow disabled.

The complete suite passes 170 tests and 44 subtests, with 22 existing Brian2 dependency warnings. The source walking controller, neural parameters and existing sensory encoders are unchanged. Geometry and interface verification do not establish a natural wind response.
