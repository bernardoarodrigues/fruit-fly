# Active implementation plan: one male fly

User scope update: 2026-09-04. Focus on **one male Drosophila** to accelerate testing; a second fly and paired reproduction are deferred. The user wants a solid simulation through the relevant planned milestones, a way to watch it, and a GitHub repository updated during development. Additional compute can be supplied when measurements justify it.

This is the active implementation plan. The research roadmap records the broader two-animal program and remains the source of scientific validation requirements. This scope update does not turn a body animation or a small substitute graph into completion.

## Current scientific priority: 2026-09-05 UTC

The [complete passive-transient comparison](../research/41-apl-passive-transient-comparison.md) fits 2,132 sweeps and all 328 mean/window combinations. Mean tau depends strongly on the window (median 9.1–18.4 ms), 221 single-sweep fits reach 200 ms, and conditional offset prediction remains imperfect. Independent mean-fit optimizations agree. Next compare a constrained positive fast/slow passive response with a single-component control and untouched offset scoring before fitting active recovery. Do not interpret a tail-fit tau as membrane capacitance or select a global value from this sensitivity panel.

The [dedicated 2 nA AHP batch](../research/40-apl-dedicated-ahp-measurements.md) is complete: eleven cells, 65 sweeps, both fixed window starts, and independent scalar/crossing checks. Control mean-trace AHP magnitudes exceed RNAi descriptively, but all eleven mean traces retain repeated threshold crossings; do not adopt their first intervals as kinetics. Next freeze the passive-transient comparison and then evaluate full recovery waveforms under a declared variation/noise treatment, with fly-level partitions and no threshold retuning.

The [complete passive-response batch](../research/39-apl-passive-measurement-batch.md) now measures all 2,132 sweeps from 82 files. All 2,002 recorded-current sweeps meet the declared ±5 pA delivery check; 130 lack a recorded pA channel. Independent arithmetic passes and 79 fixed-window/source comparisons are close without cursor retuning. Next freeze a transient comparison with onset/artifact and window sensitivity, preserve repeated-sweep variation, and analyze all eleven dedicated 2 nA AHP cells together. These measurements constrain a graded leak/SK candidate but do not select capacitance, release or network parameters.

The [complete APL workbook reconciliation](../research/38-apl-electrophysiology-workbook-join.md) now joins 78/80 author rows and preserves two unresolved cell IDs, conflicting filenames/directories and repeated nested summary vectors. Author resistance formulas yield MΩ despite Gohm headers; adjusted RMP subtracts 15.7 mV. Next run one complete protocol/measurement batch with explicit identity flags, actual command timing, source fly grouping and unmatched slots. Prioritize passive voltage differences and delivery checks; keep 500 ms and 750 ms protocols separate and all eleven dedicated 2 nA AHP cells together. Do not infer unrecorded corrections or treat repeated summaries as independent targets.

The [complete acquisition and APL header inventory](../research/37-apl-electrophysiology-inventory.md) is now finished; the download heartbeat is paused. Next join the author workbooks to all 171 APL files before freezing a measurement batch. Respect actual DAC epoch amplitudes/durations (500 ms firing-pattern vs 750 ms dedicated AHP), heterogeneous channels and unknown correction state. Preserve duplicate KC NS/SD files as unresolved source identities. Earlier acquisition-in-progress entries below are historical.

The [electrical measurement reference](../research/36-apl-electrical-measurement-reference.md) is prepared and passes six synthetic tests. Use it only after the complete archive inventory establishes channel units, correction state, real stimulus windows and cell grouping. Freeze quality floors and analysis windows before cohort comparison; do not adopt the synthetic test values as APL parameters. Preserve ambiguous/weak AHPs and the distinction between a recovery interval and exponential tau.

The [new direct APL electrophysiology source](../research/35-apl-direct-electrophysiology-source.md) supersedes further unconstrained dye-filter work. The complete public Ephys ZIP is downloading with size/MD5 verification and a reused quiet completion heartbeat. Wait for the live producer; do not restart from an observation timeout or inspect partial recordings. After completion, pause the heartbeat and inventory the entire archive, joining cell/condition/protocol identities before freezing an analysis batch. Prioritize per-cell passive −50 pA responses, graded depolarization and SK-sensitive AHP, retaining normal/deprived/recovery and paired pharmacology separately. Use actual recorded commands to resolve the Fig. 4H +2 nA versus general 1 nA description. These are mated-female ex vivo recordings; transfer to the male graph remains explicit. This is a direct electrical calibration opportunity, not a fitted release law or automatic H1 promotion.

The [direct dye-driven reporter comparison](../research/34-apl-reporter-filter-comparison.md) is complete. Four assumption sets favor τ = 0 without resolving shape mismatch; all fixed conditions are accounted for, including one unscorable secondary pair. This closes the simple linear proxy/filter test. Do not adopt its boundary time constant or normalized gain as APL physiology. Next seek independent constraints separating ATP/P2X2 transduction, natural KC→APL drive and calcium reporting before proposing a nonlinear/adaptive graded feedback candidate. Preserve the complete paired traces, both clocks and calibration/evaluation distinction. Do not repeatedly refit a generic delay or substitute another whole-network gain sweep.

The [APL/dye timecourse extraction](../research/33-apl-timecourse-extraction.md) is complete: all 80 Fig. 5 curves, graphical bounds, baseline convention and both clock interpretations are retained. The printed calcium/dye 5 s bars differ by 10.77%; approximately 5 Hz imaging and persistent ATP exposure constrain what can be inferred. Next freeze a complete dye-driven reporter comparison with both clocks and declared stimulus/observation assumptions. Keep calibration and evaluation conditions explicit; do not infer an intrinsic APL time constant from a pressure-puff command or a calcium tail. A reporter fit remains separate from electrical/release calibration and recurrent promotion. Author MAT files were acquired and verified; SciPy exposes opaque geometry classes, so raw trace properties remain unresolved rather than assumed absent.

The [APL attachment sensitivity batch](../research/32-apl-attachment-sensitivity.md) is complete: 48 static probes retain one declared nearby/nonlocal alternative alongside the nearest control. Regional means are relatively stable while individual sites and some sparsely contacted KCs are sensitive. Carry both maps into prospective local comparisons; do not silently correct the anatomy or fit a global gain to compensate. This closes the initial declared attachment stress test, not anatomical validation. Next inspect the author's retained MAT/source timing leads for Fig. 5/6 and define a reporter-compatible temporal candidate with explicit release assumptions. If only plotted traces are available, quantify extraction uncertainty and keep their imaging/stimulation protocol separate from electrical dynamics. Avoid another geometry-only sweep as a substitute for this missing physiological step.

The [static male APL spatial operator](../research/31-male-apl-spatial-operator.md) now passes its complete 36-probe batch, six focused tests and 294 reference checks. Constant-diameter and coarse-radius cases retain all contact multiplicity and disconnected components; midpoint refinement preserves the field. This closes the initial static numerical implementation, not physiological calibration. Next address attachment uncertainty and declare a source-compatible graded dynamic/observation/release candidate before fitting or recurrent intervention. Keep unsupported parameters explicit, preserve separate evaluation data and check any optimized runtime against this reference. A roughly 0.3-second static call is not demonstrated suitable for a 0.1 ms neural step.

The [male APL forest/path implementation](../research/30-male-apl-skeleton-geometry.md) supplies native neurite geometry and retained synaptic attachments. Acquisition and initial attachment are complete. Do not infer absolute membrane properties or release efficacy from the coarse skeleton or transfer the normalized source-study space constant directly into native physical coordinates.

The [APL spatial workbooks](../research/29-apl-spatial-data-comparison.md) are extracted and compared across all three sites. Shared backbone entries are reconciled rather than counted as independent samples. Preserve both author anatomical representations, site-dependent residuals and the separate APL/KC normalization when designing the next local feedback assay. Native geometry and a static reference are now available; the declared observation/release mapping remains unresolved. No fitted spread, electrical strength or default is selected by these descriptive profile scores.

The [reporter timestep reference](../research/28-kc-reporter-timestep-reference.md) now provides a verified constant-input embedding of the inspected discrete inhibitory filter. All 45 scalar cases pass; coupled variable-input dynamics remain untested and the reference is not physiological calibration. The source-workbook join awaits public supplement access, with the publisher CAPTCHA handed to the user. Preserve this access gap and continue independent graded APL/observation-mapping work if access is still unavailable. No fit or recurrent trial has been launched.

Latest completed source step: [γ reporter data and author model audit](../research/27-kc-reporter-data-and-author-model-audit.md). The author WT/KD summary workbook is acquired, extracted and independently checked; the complete primary supplement/cohort join remains unresolved. Resolve that join before freezing any local reporter fit. Keep source-faithful 30 Hz discrete timing distinct from any continuous-time alternative, and do not treat the used decay tail as untouched evaluation data. This source step launches no neural trial, changes no runtime and does not reopen the closed γ batch. Calcium/axon-release observations still need an explicit mapping before electrical or graded APL promotion.

The user's latest direction is a [staged causal navigation ladder](navigation-ladder-plan.md), with local physiological calibration before broader navigation claims. The [first stage readout](navigation-ladder-readouts.md) is complete and independently reviewed: it reuses all 60 inhibitory-panel trials across 34 cohorts, reveals strong immediate PN recruitment and persistent activity in selected MBON/DN populations, and keeps the narrow walking-output failure explicit. The [exact-spike timing diagnostic](navigation-ladder-timing.md), [VM2 depression-panel extraction](orn-pn-depression-digitization.md) and [conditional fixed-reference comparison](orn-pn-depression-comparison.md) are also complete. The subsequent [local calibration](orn-pn-depression-calibration.md) fits only the designated 20 Hz curve, retains 15/50 Hz for evaluation, and finds a conditional recovery time near 104 s after a separate ceiling check. Cross-frequency transfer remains poor and the starting recovery state is not established by the source. No parameter is promoted. The subsequent MBON input audit is complete and its selected causal intervention is described below. Further local fitting needs compatible amplitude/history constraints. No new whole-network E/I gain sweep is planned; the suggested synapse-handling decomposition is superseded.

The user explicitly cancelled the proposed embodied Eon integration benchmark. Its existing bounded neural results were independently reviewed, committed and pushed at `ab2bd63`. The [inhibitory C0/C1/H0/H1 factorial](inhibitory-factorial.md) and subsequent recurrent panel are complete. No arm is selected. Do not continue copying Eon's stimulation or fitting motor gains as the main path to realism. No embodied P9/taste benchmark is required.

The [MBON input audit](navigation-ladder-mbon-inputs.md) and its selected [KC-to-MBON delivery intervention](navigation-mbon-intervention-results.md) are complete. All six branches and independent delivery/state, numerical and outcome reviews pass. The causal effect is large but physiologically inadequate: MBON12/14 become silent, MBON13 remains highly active, matched odor contrast is negligible, and saturation returns after restoration. Whole-network persistence is not resolved. That batch is closed; do not restart it. The separate γ-KC batch below is also now complete and the reused trigger is paused.

The [KC/PN/APL inventory](pn-kc-apl-inventory.md) is also complete and independently reviewed across twelve saved histories. It finds almost universal KC activity, dominant positive KC-to-KC increments, generic fast dopamine input comparable in magnitude to ALPN input, and near-ceiling model APL events. These are distinct calibration problems, not a reason for another global gain sweep. The subsequent [five-arm single-event KC comparison](kc-synaptic-response.md) exposes a conditional rise/decay tradeoff in a minimal current-plus-membrane cascade; neither fast candidate satisfies the separate somatic time-constant constraint. No local parameter is installed. Next compare a source-supported mechanism that separates somatic and synaptic responses, retaining the published [finite-transmitter conductance equations](pn-kc-waveform-candidate-review.md) as an explicitly historical control and using [same-version contact locations](../research/23-kc-contact-location-availability.md) to constrain any spatial input partition. The [APL source review](../research/22-apl-local-feedback-constraints.md) does not yet supply a graded release law. Exclude threshold/APL compensation and source-free compartment parameters disguised as measurements. The frozen [PN→KC/APL plan](pn-kc-apl-calibration-plan.md) remains the prior methodological record; source-specific criteria and untouched evaluation data must be fixed before future fitting.

The [fixed Turner conductance reference](turner-kc-conductance.md) and [same-version spatial join](kc-apl-contact-locations.md) are now complete and independently reviewed. The reference approximately matches the authors' intended amplitudes but retains timing/intrinsic limitations. The spatial table reconciles every modeled edge and distinguishes largely CA-labelled PN input from largely lobe/PED-labelled KC recurrence. The [receptor source review](../research/24-kc-spatial-mechanism-constraints.md) supports a γ-specific model-dependence diagnostic: test the assumed fast positive γ-lobe KC contribution, preserving other regions of mixed-ROI pairs. The biological reporter evidence does not identify an electrical synaptic kernel. A delivery intervention would not be a biological sign flip, deletion of anatomy or fitted GPCR law. Keep the current control, exact source/target/ROI scope, partial-edge contact handling and actual recurrent histories explicit before launching a new batch; any whole-lobe extension is a separately declared broader diagnostic.

The [contact-aware γ-KC diagnostic](kc-gamma-intervention-results.md) is complete and all six trials have been analyzed together. It cuts γ pulse firing by 54.17–54.53% and consistently increases a small local odor contrast, but APL/MBON saturation, silent DNg97/DNp09 and post-input population persistence remain. Independent state/delivery and all 89,995 numerical interval checks pass; a separate whole-trial sorting reduction verifies counts and ISIs. This is a completed model-dependence test, not a validated receptor replacement. The existing heartbeat is paused; do not restart or repeat the batch. [Design notes](../research/25-kc-gamma-diagnostic-design.md) and [derived findings](../research/26-kc-gamma-recurrent-findings.md) preserve confounds. Next inspect the retained public γ-KC reporter data/protocols and author-code leads, while keeping APL's graded release mapping unresolved; match observables before fitting. No broader lobe removal, whole-network gain sweep or body integration is planned as a substitute for physiological calibration.

H1 must not be selected because its voltage plot looks better. Before promotion it must satisfy numerical bound and accuracy checks, relevant physiological amplitude/timing comparisons, meaningful stimulus contrasts, seed robustness, and improved recurrent full-network behavior without uncontrolled persistence. Conditional fixed-input replay cannot satisfy the recurrent gate. Record failed and unresolved criteria explicitly; see [operational promotion gates](inhibitory-promotion-gates.md).

The [recurrent implementation/performance check](inhibitory-recurrent-performance.md) completes all five 50 ms runs with 280 checks and 499 independent saved-data checks. C0 matches the original full endpoint state exactly; altered arms generate their own distinct recurrent histories. The [matched four-thread check](inhibitory-recurrent-parallel-performance.md) passes 1,242 checks with exact retained-array parity and lower measured H integration time. The [three-seed stimulus/withdrawal panel](inhibitory-recurrent-panel-results.md) completed all 60 trials and its independent batch review; it was committed and pushed at `94826f2`. Do not restart it. Its completion heartbeat is paused. Neither numerical bounds nor retained local contrasts overcome the persistent-activity and physiological-calibration failures, so the recurrent result does not promote H1.

Still-air odor ON/OFF behavior, odor-plus-wind orientation and fed/hungry state effects are separate assays. Future neural batches must have a fixed complete condition set and be analyzed together after the quiet completion trigger, as requested by the user. Source-rate declarations, hDelta identities, circular coordinates and decoder assumptions must remain visible in their records.

The [5 ms filter comparison](ln-inhibitory-linear-filter-results.md) and its [independent numerical review](ln-inhibitory-linear-filter-independent-review.md) reject the declared direct mapping between the displayed rate and current summaries: the bounded filtered-rate change is negative while the current change is positive. Different cohorts, acausal smoothing and unresolved circuit identities prevent interpreting this as receptor or full-network rejection. No gain or time constant was fitted.

BANC is available as a female CNS structural comparison; the [retained metadata audit](../research/17-banc-comparative-circuit-insights.md) informs identities and circuit hypotheses without supplying missing electrophysiological parameters or reopening the deferred paired-animal scope.

## Required milestones and evidence

| Milestone | Required implementation | Completion evidence |
|---|---|---|
| M0 Provenance/repository | GitHub repository; pinned code/data; retained notes; no hidden downloads/filters | Remote commit exists; manifests, hashes and licensing notes agree with actual imports |
| M1 Single physical fly | One body; controlled arena; food/water/terrain; documented intended-male morphology profile | Stable reproducible trajectories, contacts, units, controller parity and declared anatomy gaps |
| M2 Sensory pipeline | Bilateral odor, contact taste, vision/proprioception; appropriate stimulus channels and state | Numeric calibration/geometry tests; input replay; no privileged source coordinates enter neural policy |
| M3 Neural replication | Persistent LIF engine checked against Shiu/Brian2 equations and selected published circuit experiments | Independent numerical comparison; delay/refractory/chunk/reset tests; published assay reproduction recorded separately |
| M4 Male CNS import | Full retained MaleCNS neuronal graph with explicit transmitter/sign assumptions | Dataset inventory/filter audit, all selected neurons retained, connectivity and source checksums |
| M5 Closed sensorimotor loop | Actual male neural activity drives the physical body's motor interface | Causal ablation, shuffled/control outputs, stable dynamics, timestep/coupling checks, synchronized clocks |
| M6 Foraging/feeding/homeostasis | Sense food, approach/stop/feed, deplete food, maintain separate energy/water/fullness | Held-out layouts/plumes, input dissociations, intake/resource conservation and neural dependence |
| M7 Single-animal repertoire | Grooming, escape/avoidance, rest/sleep/circadian state, learning; male social-cue/song assays where feasible alone | Per-behavior source/assay, quantitative comparisons, interventions and documented surrogate components |
| M8 Mechanistic motor expansion | Assess/refine VNC–motor–actuator pathways against retained controller baseline | Better neural/kinematic held-out predictions; documented remaining motor/muscle approximations |
| M9 Watch/control/record | Browser or application viewer showing the actual current simulation; pause/reset/camera/stimulus/ablation controls | Live UI inspected; controls verified against process state, frames and recorded trajectory; errors surfaced |
| M10 Robustness/performance | Reproducible runs, automated focused tests, profiling and compute recommendation | Longer-run checks, CPU/memory/full-loop measurements, reproducible launch from repository |

Flight/lifespan expansion remains part of the broader roadmap and must be assessed after the walking loop. A flight requirement cannot be supported by a walking-only check. Paired courtship, copulation, sperm transfer to a female, oviposition and offspring require the deferred second-animal stage; do not fake them as single-male behaviors.

## Scientific boundaries

- The available body is female-derived; intended-male configuration must disclose this until measured male morphology is implemented. A filename or changed color is not male anatomy.
- The male connectome is a measured structural scaffold with incomplete synaptic attachment; LIF dynamics and many signs/parameters remain model assumptions.
- A trained/CPG/reflex walking controller is a declared motor surrogate. Decisions must depend on recorded neural outputs for neural-control claims.
- Engine numerical parity, published-circuit replication, and biological whole-animal fidelity are separate validation levels.
- Any module with engineering constants must state them as such. New hypotheses remain unvalidated until appropriate experiments support them.

## Execution record

Initial state: research dossier and two-body/controller smoke examples only; no neural engine, male graph import or viewer existed. This implementation phase begins from that evidence. Record progress and remaining gates in `docs/STATUS.md` with current result paths and Git commits.
