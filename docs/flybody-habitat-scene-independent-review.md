# Independent review of the habitat scene correction

The saved-data review passes **756 checks**. It independently reconstructs all four final scenes, compares **7,360 surviving geometry fields**, and verifies **74,841 actual float32 actor-input values across 101 native states**. No simulation, controller, neural model, render, or contact probe was rerun by the reviewer. The pass means the retained display and parity evidence is internally consistent; earlier physical and visual failures remain failures.

The final executed source is **`62129a3`**, with execution-plan SHA256 `14229a078298ce3ba43761a05aed602ff7d2463ffa727d4ea3b7afb85795edf2`. The earlier alpha-only renderer is **`ef6e230`** and original contact implementation is **`fd19b56`**. The review resolves source through these Git revisions, so later workspace changes do not silently substitute a different implementation. The [review receipt](../validation/flybody-habitat-scene-independent-review.json) records source and input hashes, all checks, scene changes, pixel comparisons and limitations. The earlier [native contact review](../docs/flybody-habitat-independent-review.md) and its receipt were left unchanged.

Each saved scene contains 228 objects before the callback and 92 afterward. Exactly **33 ghost geometries and 103 trajectory sites** disappear. Expected removal membership comes from the separately retained v1 edit IDs; the final filter's own removal list is then checked against those IDs and the original scene indices. The compiled-name selection in pinned source uses `ghost/` geometry names and `traj_` site names. A complete compiled ID-to-name inventory was not retained, so that classification is supported by source inspection and the earlier edit record rather than independently reconstructed native model names.

The reviewer extracts the 20 public `MjvGeom` fields from the installed MuJoCo 3.2.7 C header, independently of the producer's Python field list. Every surviving field matches exactly after two specified adjustments: each resource marker's Z increases by **0.005 native cm = 0.05 mm**, with float32 rounding, and its segmentation index follows its compacted scene position. The two resource sites retain native object IDs 500 and 501; positions in X/Y, sizes, materials, transparency, reflectance and every other public field remain exact. No reference ID survives. Forward compaction writes only already-consumed scene slots and copies the complete public geometry record. The callback never writes native model/data fields. Relative to `fd19b56`, the only changed worker function is `render`; the habitat's existing functions change only in `bind`, which records display IDs and metadata. The default habitat-null branch still constructs a camera without a callback.

All four render records retain identical before/after hashes for the full serialized compiled model, nine explicit native arrays, cached observations, actual actor input and native time. The earlier paused raw-array archive reproduces all nine final recorded array hashes, and its actor bytes reproduce the final paused actor hash. **The final per-render MJB bytes and raw native arrays were not archived**: full-model byte equality is an executed producer comparison corroborated by retained hashes and inspected serialization source. Cached observations likewise have hash evidence. The final habitat-null invariance check is a producer boolean plus the inspected unchanged default path, without a separate saved raw before/after pair.

After the four paused renders, every original saved state field at reset and all 100 subsequent 2 ms walking intervals matches the original habitat trial. All 101 × 741 actual actor values match exactly, including per-row SHA256; clocks, counters, finite states and termination flags agree. This covers the retained 0.2 s prefix, not arbitrary future commands or all possible cameras.

The **26/27 v1 renderer receipt remains failed** on `default_source_camera_pixels_exact`. From the separate repeat diagnostic's saved PNGs, the reviewer recomputes three differing pixels and a maximum one-level channel difference between its first image and each of the next four. Those next four images are mutually identical, including same-worker repeats. Saved poses and the diagnostic's seven geometry fields agree. Its native/model invariance is recorded as booleans. The original failed default image pair was not saved, so this later diagnostic does not establish the size or cause of that original mismatch.

Independent offline inspection of the saved final [overview](../validation/flybody-habitat-viewer-final/overview.jpg), [follow](../validation/flybody-habitat-viewer-final/follow.jpg) and [side](../validation/flybody-habitat-viewer-final/side.jpg) images finds smooth resource regions, the actual fly and its floor reflection, with no visible reference-dot tracks. The retained v1 follow image visibly contains dotted tracks. Alpha zero had not removed their shadow contribution; final scene removal corrects these inspected views. This reviewer inspected saved images, not the live browser.

All final saved camera snapshots are paused at zero with zero image and neural clocks. Seven scientific telemetry groups—pose, senses, physiology, neural, stimuli, physics and behavior—match the v1 reset exactly. Body metadata differs only in its worker log path, habitat module hash and the new display-filter declaration. The earlier actual controller-only wall failure remains at tick 384 / 0.768 s. Its API diagnostics exactly match the saved failure journal and original native endpoint. The last image is **0.750 s**, last completed telemetry **0.760 s**, and physical/neural failure clocks **0.768 s**; the stale image is not presented as the failure state. The original 64/65 contact and 29/31 shallow-contact receipts remain unsuccessful on their stated gates. This rendering correction provides no evidence of obstacle avoidance, indefinite containment, natural foraging or physiological fidelity.

Reproduce this saved-data review from the repository root, with retained ignored `runs/` archives and the installed MuJoCo header available:

```sh
.venv/bin/python scripts/review_flybody_habitat_scene.py
```

The script imports NumPy and Pillow only for array/image comparisons, reads pinned Git source and retained artifacts, and writes only its independent review JSON. Its input hashes are checked again before completion. The image-inspection note records the completed offline visual review; rerunning the script recomputes numerical checks and does not conduct a new visual inspection.
