# Panel input, C0 mapping and metric integration review

This review covers the incoming [panel runner](../scripts/experiment_inhibitory_recurrent_panel.py)'s input schedules, source/cohort identity, C0 original-trial mapping and streaming analysis. Resource/archive/dispatch/failure behavior and numerical reference handling have separate reviewers. The earlier [prospective preflight note](inhibitory-recurrent-panel-preflight-review.md) remains unchanged.

The [pure audit](../scripts/review_inhibitory_recurrent_panel_inputs.py) extracts only the runner's random-stream function, job-order and schedule statements, C0 comparison statements and final analysis block. It never imports the runner or calls `prepare`, `trial`, `run_panel` or a neural constructor. It compares all seeded draws/source mappings to retained inputs and uses manufactured arrays for C0 cursor/window arithmetic and missing-window analysis. No graph simulation occurs.

Source inspection confirms that all rate transitions coincide with 50-tick chunk boundaries, all 36 columns remain present through off periods, and C0 merges the new startup/baseline counts correctly: `(counts[0]+counts[1], counts[2], counts[3])` reproduces the original three windows. Cohort indices are joined from the pinned original IDs, and the streaming helper's explicit chunk bounds and summary APIs match the runner's call sites.

Two analysis integration points were flagged and corrected before panel freeze: completed-count coverage now follows a successfully retained prefix, and clock-complete summaries from correctness/retention/interruption failures remain raw evidence but are excluded from paired outcomes with explicit reasons. An empty eligible map reports unavailable comparisons. Manufactured checks cover these cases, including a resource-limited trial whose completed pulse window remains usable while its partial off window remains unavailable.

The first execution passed **138 of 138 checks** in the [input-review receipt](../validation/inhibitory-recurrent-panel-input-review.json). The runner SHA256 was `597eef47d93f3b57896fa7c38864ea8003153f8e54d1de720f2efc23f2bf2512`; all 37 audited input hashes remained unchanged. The audit checked 3,240,000 independently reconstructed random draws, all 15 original C0 input/RNG mappings, all 60 job positions, the fixed seven windows and exact source/first-hop/target/motor cohort joins. Manufactured C0 records checked ordered-spike cursors and startup/baseline merging without replaying a neural model.

The audit script SHA256 is `5afb7ce62e23a2e113795d893919e722f6232411ff28b93734eeb5d92b8d4cde`; the executed receipt SHA256 is `1c40c651dec97d3d95e495846ce28ed12fdf8e4e73d6f11c661c87c39849919f`.

The final [aggregate preflight receipt](../validation/inhibitory-recurrent-panel-preflight-review.json), SHA256 `206403f73f4d964a47b4750b85f91ac438aeec8064fd48c5d720d45ad02cc071`, records **seven passing components and 666 checks**, including the independent archive decoder. It pins the runner, kernel, archive, metric and solver sources and all component receipts. The first aggregation attempt encountered a list-versus-mapping schema error before publishing a gate; its [failure record](../validation/inhibitory-recurrent-panel-preflight-assembly-attempt1.json) is preserved. No component test failed or reran. The original six-component aggregate is retained as [version 0](../validation/inhibitory-recurrent-panel-preflight-review-v0.json); the final receipt documents the addition of the existing independent archive review.

The source preflight passes within these review scopes. It does not replace the real 15 C0 prefix gates, establish intermediate global-state parity, or demonstrate physiology, persistent-activity control, seed robustness or H1 promotion. No 60-trial panel simulation was executed by this review.
