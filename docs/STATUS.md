# Single-male implementation status

Evidence snapshot: 2026-09-06 UTC. This follows [PLAN.md](PLAN.md). Full-graph execution, numerical correctness and biological fidelity are distinct results.

**Experimental graded electrical state:** [exact state updates and the full conditional active-recovery comparison are implemented](../research/43-apl-graded-state-and-active-recovery.md). Active recovery improves four of five evaluation flies across all four fixed passive variants; evaluation mean-cell RMSE falls from 0.91–0.95 mV to 0.48–0.52 mV. Four ODE/subdivision tests and independent checks of all 88 prediction traces pass. Outward-state recruitment is still prescribed, so the module remains experimental and is not wired into neural runtime defaults.

**Positive passive response candidate:** [all 656 single/two-mode fits are complete](../research/42-apl-positive-passive-modes.md). Two-mode median conditional switch-off error is 1.74–2.26% of the passive step, versus 5.91–6.43% for single-mode controls; 80–81/82 files improve per window. Both numerical reference tests and all independent mean-fit/prediction checks pass. The two-mode family advances to local active-response modeling with all window variants retained; no runtime or biological parameter promotion occurs.

**Complete passive transient comparison:** [2,132 sweep fits and 328 mean/window fits are complete](../research/41-apl-passive-transient-comparison.md). Window-dependent median tau spans 9.1–18.4 ms; 221 sweep fits reach the upper bound. Small onset residuals do not assure conditional switch-off prediction. All 328 independent mean-fit optimizations agree and the complete figure is reviewed. No membrane time constant, capacitance or runtime parameter is promoted.

**Complete dedicated AHP measurements:** [all eleven cells / 65 sweeps measured together](../research/40-apl-dedicated-ahp-measurements.md). Control and RNAi mean-trace AHP medians are 3.464 and 1.219 mV, with overlap. All eleven means retain repeated recovery crossings; no time constant is selected. All 42 recorded-current sweeps meet the declared 2 nA delivery tolerance; 23 are command-only. Independent measurement checks pass and all-cell traces are visually reviewed.

**Complete raw passive measurements:** [82 files and 2,132 sweeps measured together](../research/39-apl-passive-measurement-batch.md). Recorded-current checks pass for all 2,002 available sweeps; 130 retain command-only estimates. Across 79 author comparisons, the fixed-window mean differs by median −3.37 MΩ. All 10,400 independent arithmetic checks pass, and the complete figure is reviewed. Identity conflicts and within-file variation remain explicit. This supplies a direct passive-response reference, with no model parameter promotion.

**APL workbook reconciliation:** [all five workbooks are extracted and independently checked](../research/38-apl-electrophysiology-workbook-join.md): 78/80 author observation rows join exactly to files; two IDs and several conflicting names remain explicit. The NS/SD/RS amplitude, recovery and paired-drug counts reconcile with the reported cohorts. Resistance values are MΩ despite a Gohm header; adjusted RMP uses a 15.7 mV subtraction. Two nested workbooks repeat the same 37-value vector. Source XML and 164 formula calculations pass verification; no raw voltage fitting or parameter promotion occurred.

**Complete electrical source inventory:** [all 1,227 archive files passed integrity checks and all 171 APL ABF headers parsed](../research/37-apl-electrophysiology-inventory.md). The completion heartbeat is paused. Actual epoch tables distinguish 500 ms firing-pattern pulses from dedicated 750 ms AHP pulses and confirm 2 nA SK-RNAi/control commands. Heterogeneous channels, extra candidate records and 16 duplicate KC ABF pairs across NS/SD folders require source-workbook joins before comparison. No voltage fitting or parameter promotion occurred.

**Prepared electrical measurements:** [passive-step and AHP measurement functions](../research/36-apl-electrical-measurement-reference.md) pass six tests against constructed waveforms while the source archive downloads. They preserve holding offsets, explicit units/windows, flat/weak signals, incomplete recovery and repeated threshold crossings. The source's 70%-to-30% recovery interval is kept distinct from an exponential time constant. No biological data or partial archive was analyzed; source grouping and quality rules remain pending.

**New direct electrical source:** [Chen et al. 2026 APL recordings](../research/35-apl-direct-electrophysiology-source.md) provide a public 2.3 GB raw electrophysiology archive. The verified live acquisition is recorded in `validation/apl-sk-ephys-launch.json`; a quiet completion heartbeat will wait for full checksum verification before inventory. The study reports graded, non-spiking APL responses, approximately 120 MΩ input resistance and SK-dependent AHP. These are ex vivo mated-female recordings, with holding/correction and 1 nA versus 2 nA protocol issues to reconcile. Raw files are not yet analyzed. This direct source takes priority over more free reporter-filter parameters.

**Latest reporter comparison:** the [complete dye-driven filter test](../research/34-apl-reporter-filter-comparison.md) fits two declared C traces and evaluates D/E under both clocks and signed/nonnegative proxies. All four fits favor the instantaneous boundary while substantial shape mismatch remains; this does not identify instantaneous APL kinetics. The fixed inventory retains 312 scored predictions and eight explicitly unscorable slots from one negative-peak secondary dye trace. Two integration tests, 1,896 saved-summary checks and eight independent gain solves pass. No parameter is promoted. Next separate experimental stimulus transduction from reporter dynamics using independent source constraints before defining graded feedback.

**Latest timing reference:** all [80 displayed APL calcium/dye curves](../research/33-apl-timecourse-extraction.md) are extracted from Amin Fig. 5 with two focused tests, 720 analytic/subdivision reference sections and visual comparison. The two printed 5 s bars differ by 10.77%, so both time coordinates and graphical bounds remain saved. Approximately 5 Hz imaging and persistent ATP exposure prevent equating these traces with intrinsic membrane/release kinetics. Two author MAT files are checksum-verified but expose opaque geometry class objects rather than a directly accessible trace table. Next compare a declared dye-driven reporter candidate under both clocks, before any electrical or recurrent promotion.

**Latest geometry sensitivity:** the [nearby-branch stress test](../research/32-apl-attachment-sensitivity.md) completes all 48 probes, including 12 controls, before combined analysis. The declared alternative affects approximately 16% of KC→APL and 11% of APL→KC contacts. Global mean changes remain below 0.5%, but some individual sites and sparsely contacted KCs change substantially. Controls reproduce the preceding batch, exhaustive reference searches pass, and a separate reduction verifies 432 saved summaries. Both maps remain sensitivity scenarios; no anatomical correction or runtime change is applied. Next inspect the identified source timing-data leads and define a reporter-compatible temporal/release comparison.

**Latest implementation prerequisite:** the [static male APL spatial operator](../research/31-male-apl-spatial-operator.md) completes all 36 anatomical probes together. Six focused tests and 294 reference checks pass, with direct shortest-path agreement and midpoint subdivision errors below 3.3 × 10⁻¹⁵. The radius assumption changes contact-weighted mean coupling by 15.72–32.34%; contact multiplicity also materially affects summaries. These are declared regional unit-input probes, not physiological stimulation. Attachment uncertainty, time dynamics, release calibration and runtime performance remain open before recurrent integration. No neural or body default changed.

The preceding [native male APL geometry](../research/30-male-apl-skeleton-geometry.md) includes both checksum-verified SWC forests and exact segment projections for 439,226 retained contact endpoints. Five focused geometry tests and 2,477 independent reference comparisons pass. All KC-related endpoints attach to the main components; five other incoming endpoints attach to disconnected fragments, preserved without invented bridges. Coarse radii and approximately 0.6–0.7 µm median attachment residuals limit precise cable interpretation.

**Latest APL evidence:** the [published spatial-data comparison](../research/29-apl-spatial-data-comparison.md) verifies 7,550 numeric cells and 74 formula caches, retains 5,640 Fig. 7 observations, and compares all 18 site/model/spread combinations together. The preferred exported spread depends on stimulus site and anatomical representation. APL calcium and downstream KC suppression remain distinct measurements, with calyx P2X2 leak controls required. The final comparison figure is visually checked. No refit, new neural simulation or runtime change occurred. Next establish a source-supported APL spatial operator and separate reporter/release mappings before testing graded recurrent feedback.

**Latest numerical prerequisite:** the [isolated reporter inhibition reference](../research/28-kc-reporter-timestep-reference.md) passes all 45 scalar cases and 362 checks, including affine-map, timestep subdivision and adaptive ODE comparisons. It preserves the author recurrence for constant inputs at a fixed reference step, without assigning physiological meaning to its coefficients. Naive 0.1 ms substitution can greatly change inhibitory gain and memory. The source-cohort join remains open: API access excludes the manuscript, Chrome blocks the PMC supplement download, and the publisher page awaits human CAPTCHA verification. No fit, new network run or runtime change was made.

**Latest source audit:** the [γ reporter workbook and author model](../research/27-kc-reporter-data-and-author-model-audit.md) are inspected and pinned. All 1,996 numeric cells independently match their XLSX encoding. The export contains WT/KD means and SE, without individual responses or timestamps; exact primary cohort/normalization remains unresolved. The author implementation models calcium, averages a selected synthetic responder population, and has timestep-sensitive inhibitory memory gating. It cannot directly calibrate male electrical or graded APL dynamics. Two PMC supplement requests returned retained HTML challenges. No fit or new neural batch ran; next resolve the source-data join before freezing a reporter-level comparison.

**Latest completed batch:** the [contact-aware γ-KC diagnostic](kc-gamma-intervention-results.md) completed all six trials and its combined reviews. γ pulse rates fall from about 340 to 155 Hz and a small odor contrast increases across all three seeds, but saturated APL/MBON firing, silent DNg97/DNp09 and global post-input persistence remain. State/delivery checks, all 89,995 declared numerical intervals and an independent all-population count/ISI reduction pass. H1 is not promoted. The completion heartbeat is paused; do not restart this batch or repeat per-trial analysis. Next inspect the retained public γ reporter data/protocols and unresolved graded APL mapping before any new physiological fitting or recurrent intervention. See [derived findings](../research/26-kc-gamma-recurrent-findings.md).

Supporting anatomy: the [KC/APL spatial join](kc-apl-contact-locations.md) is complete and passes 40,954 independent checks, including a full source rescan. All 836,650 modeled edge counts reconcile exactly. ALPN→KC contacts are 87.665% CA-labelled, while KC→KC contacts are 82.828% lobe-labelled and 7.182% PED-labelled; all unspecified and outside-graph records remain retained. The parallel [fixed Turner conductance reference](turner-kc-conductance.md) passes 204 independent checks and approximately reproduces the authors' intended event amplitudes, while retaining rise-time and intrinsic somatic mismatches. These results constrain a location- and receptor-specific mechanism comparison; they do not identify a calibrated replacement for the dense recurrent KC activity. H1 and the negative depression fit remain unpromoted; Eon body integration remains cancelled. No runtime, sensory, decoder or body parameter changed.

The [receptor source review](../research/24-kc-spatial-mechanism-constraints.md) finds direct γ-axon mAChR-B-dependent modulation of calcium, ACh release and cAMP, without an identified unitary electrical kernel. The completed [contact-aware γ-KC diagnostic](kc-gamma-intervention-results.md) retains 414,095 selected contacts, 46,992 preserved contacts on affected pairs, six historical H1 branches and 73 observed cells. It establishes substantial model dependence on the fast positive term, but fails the recurrent/physiological promotion gates. The subsequent reporter-data/code audit above closes the initial inspection step while retaining the full-supplement and electrical-mapping gaps.

| Gate | Current evidence | Remaining work |
|---|---|---|
| M0 Repository/provenance | Private GitHub repository and initial push; source/processed checksums; dependency records | Continue versioning milestones |
| M1 Physical fly | Articulated MuJoCo body, actual contacts, arena, finite food/water, deterministic reset; controller parity | Female-derived body; measured male morphology work ongoing |
| M2 Sensors | Bilateral odor; leg-specific sweet input; optional 137-cell FeCO club proxy; actual compound-eye samples; local antenna airflow and verified female wind measurements | Wind-to-neuron and spatial/graded visual mappings; water/labellar/pharyngeal inputs; sex transfer, hook/claw tuning and receptor calibration |
| M3 Neural replication | Independent numerical tests; exact full Shiu630 spike replay for 100 and 1,000 ms; 90 sugar/MN9 trials | Published biological effect-size comparison and further circuits |
| M4 Full male graph | 166,700 neurons; 25,582,938 directed pairs; 124,177,617 contacts; 217 isolated selected neurons retained | Receptor-specific signs and physiological strengths remain assumptions |
| M5 Neural/body loop | Continuous full graph; DNg97 stimulation moves body; motor-readout mute suppresses movement while spikes continue; clock/reset/chunk checks | Sensory-driven calibrated movement, circuit interventions, shuffled outputs, coupling sensitivity |
| M6 Foraging/homeostasis | Actual tarsal sugar→full graph→feeding assay; removing taste, blocking sweet synaptic output or muting readout stops intake; separate nutrient/water conservation | Odor approach remains a negative result; ingestion abstracts proboscis mechanics |
| M7 Repertoire | Rest/walk/feed; optional neural-gated measured grooming template with contact and mute/withdrawal controls | Natural sensory grooming, visual escape, sleep/circadian state, learning, male cue/song assays |
| M8 Motor mechanism | Full retained VNC participates in neural graph; frozen CPG/reflex body adapter | Motor-neuron-to-actuator calibration and refinement |
| M9 Watch/control/record | Actual full-graph viewer inspected; pause/reset/camera/motor mute verified; assay and body caveats visible | Extend display with validated modules |
| M10 Robustness/performance | Focused tests, exact replay, active full-graph benchmark, three 10-second full-loop runs | Held-out/perturbed behavior; locomotor restart isolated from feeding; wall contacts; voltage plausibility |

## Executed loop controls

All conditions used the actual male graph, seed 1, 0.1 ms neural/physics steps and 5 ms coupling, for one simulated second. [Results and frames](../validation/closed-loop/results.json) are retained.

| Condition | Horizontal displacement | Evolution + telemetry wall time |
|---|---:|---:|
| Environmental odor | 0.000723 mm | 5.90 s |
| Odor + direct DNg97 40 Hz stimulation | 5.441 mm | 7.63 s |
| Same stimulation, motor readout muted | 0.000723 mm | 5.99 s |

Clocks remained synchronized, arrays finite, and resource residuals below 1e-8 normalized units. Muting affected only the motor interface. This establishes decoder dependence in a direct stimulation assay, not natural sensory-to-walking behavior.

**Finite values are insufficient physiological validation.** The [motor audit](motor-calibration.md) found mean walking-neuron potentials down to roughly −164 mV after transferring current-based Shiu parameters/sign rules to MaleCNS. Positive graph paths and strongly negative readout voltages coexist. Subsequent bounded-inhibition experiments did not restore the selected walking outputs, so those original observations do not uniquely localize the navigation failure to inhibition. The replicated original model is retained alongside experimental alternatives.

The [conductance extension](conductance-model.md) is now implemented and independently checked. Its 17-condition sensitivity screen preserved voltage bounds but showed strong gain dependence and continued recurrent activity after odor input stopped. Neither bounded voltage nor that persistence demonstrates validated behavior or memory. The fixed exponential reference remains the default for this optional backend; a recorded Padé option and matched-event timestep comparisons expose the speed/accuracy tradeoff.

The [taste contact assay](../validation/taste-contact/results.json) ran 0.5 s with odor disabled and actual feet on food. Sweet input produced 0.009432 normalized intake units. No taste input, motor-readout mute, and blocked sweet-sensor outgoing synapses each produced zero intake; the blocked sensory cells still fired. This tests a neural feeding interface with abstract tarsal ingestion, not mouth/pump mechanics.

The subsequent [feeding-motor audit](feeding-motor-expansion.md) records all 48 annotated proboscis motor cells plus two Fdg candidates. The existing taste assay recruits only part of the oral program. A presumed equality of afferents under motor muting failed because movement changes foot contacts; the failed result is retained. Recorded-input replay reproduces both baselines exactly and isolates a candidate Fdg contribution to MN9 recruitment. No swallowing or new intake model is claimed.

The [wind dataset](wind-calibration.md) was downloaded by the user, checksum-verified and selectively extracted. All 17 paired female antennal response means are retained, with derived fits and leave-one-fly-out error. [Airflow geometry](airflow-geometry.md) uses actual antenna velocities and a retained head-geometry frame; it detects out-of-domain empirical estimates. A MuJoCo-fused head-body lookup was caught and corrected before the reference assay; antenna body IDs used by existing odor sensing were valid. Wind-to-neural transduction remains uncalibrated, with important [cell-identity, nonspiking and peptide-signaling limits](wind-neural-mapping.md).

A [broad ORN recruitment diagnostic](odor-recruitment.md) also failed to restore reliable motor readouts. The [held-out DN decoder test](dn-decodability.md) failed sensory-contrast generalization. Neither result was used to install a steering policy. The independent [slow navigation-circuit reproduction](navigation-memory-model-audit.md) matches a pinned author's model at two parameter points, but is not integrated as validated MaleCNS memory.

An [executed multisensory motor probe](../validation/multisensory-smoke.json) includes actual compound-eye samples and leg-velocity-driven club input. Eye-image brightness responds to illumination changes, and reset restores the initial samples. Zero lighting leaves the renderer's fixed background radiance; it is not total darkness. The viewer reports elapsed simulation/wall time including rendering and pacing and labels both the assay and currently unmapped visual pathway.

The [full-graph grooming assay](grooming-loop.md) completes one measured traversal under direct left-DN stimulation, with 0.408 s of physical antennal contact and 2.151° joint tracking RMS. No-input and motor-mute controls produce no playback or contact; withdrawal cancels playback and tonic input does not loop it. The female-derived trajectory, rigid antennae, engineering motor adapter and unphysiological Shiu dynamics remain explicit limitations.

The subsequent [JO-F sensory activation assay](grooming-sensory-loop.md) reaches the same body interface without direct DN stimulation. All 18 two-seed checks pass. Sensory-output blocking preserves the exact stimulated source spike trains and eliminates all downstream spikes and playback. Both sides' grooming DNs respond, and the inputs are imposed events with an uncertain crosswalk to experimental drivers. This is a causal model demonstration, not calibrated natural touch or side-selective grooming.

The [free-running data audit](freewalking-data-alignment.md) discovered that native 800 Hz poses and stored interpolated velocities share array lengths but refer to different times. Independent reconstruction confirms the mismatch in all 372 bouts. Per-fly sex and exact absolute source-frame origins are not resolved. Aligned derivatives and mixed-sex labels are required before this dataset can support motor calibration.

The [mixed graded/spiking backend](graded-model.md) now supports exact nonspiking cell populations without removing their graph edges. Independent equation/delay/checkpoint checks and a full-graph 16-candidate APN2 smoke assay pass. Release parameters remain explicit hypotheses; no wind physiology fit or body/viewer integration is claimed.

The [walking benchmark](freewalking-benchmark.md) now derives aligned kinematics from 372 bouts and compares the actual physical controller and neural motor probe. It identifies fixed cadence, undersized excursions and reversing body yaw as separate calibration targets, while preserving the source's mixed-sex and curated-running limits. The subsequent [20-trial CPG experiment](cpg-calibration-experiment.md) reduced excursion error but failed its combined speed/yaw promotion gates. No candidate changed the runtime defaults.

The [visual-column audit](visual-input-mapping.md) found released downstream column labels and tentative connectivity-based R1–R6 assignments, with incomplete and unequal eye coverage. [Camera ray footprints](retina-ray-calibration.md) now reproduce the actual pixel pipeline and pass fourteen independently rendered geometric checks. Camera-to-anatomy registration and graded phototransduction remain unresolved; no visual neural adapter was installed. The [wind transduction review](wind-transduction-boundary.md) likewise separates measured deflections/voltages from the unidentified peripheral rate and release laws.

An isolated [PER/TIM oscillator reproduction](circadian-lg1998.md) matches the published 24.135-hour model period and entrains under its specified light/dark forcing. [Independent equation and numerical review](circadian-independent-review.md) agrees. Biological-hour units and tentative concentration units are explicit. It is not coupled to the fly's neural/physical clock, light sensor, or sleep.

The released [FlyBody walking policy](flybody-inference-trial.md) loads in a separately pinned Apple Silicon CPU environment with verified observation/action dimensions and an independently checked mean network. Its [fixed six-condition comparison](flybody-motor-comparison.md) reduces straight reversing yaw by 75–83% and follows both commanded turns, but fails the declared zero-command and withdrawal speed gates. Repeated seeds produce identical physics, and aggregate joint-ROM error does not improve. This is a comparison of complete motor/body stacks. No runtime replacement is promoted.

The [anatomical visual audit](visual-retinotopy-feasibility.md) exactly joins 13,267 released right-side visual cells to the retained male graph and extracts 852 geometric optical directions from a female micro-CT template. Their male registration remains unresolved. The [camera-coverage study](visual-template-coverage.md) finds 69 template axes outside the current right camera and another 79 inside its aperture but absent from the pixel resampling. No nearest-facet substitution or visual neural drive was installed.

The isolated [six-view sampler](multiview-eye.md) now acquires all 852 axes from the actual moving eye origin/head frame, with 38 geometric target checks and a world-light/RGB seam control. Its four-pixel interpolation is a numerical point-query approximation, not an ommatidial acceptance kernel. The existing arena has no world lamps, so disabling camera-attached illumination leaves its non-emissive surfaces dark; that result is recorded explicitly. No visual neural mapping or default camera change is installed.

The [FlyMimic muscle audit](musculoskeletal-feasibility.md) compiles the source-native 15-MTU left-foreleg model and verifies its mechanical responses. Its fast activation time constants produce numerical overshoot at the source Euler timestep. Force units and exact male motor-unit identities remain unresolved; this does not replace the six-leg body. An [independent feeding review](feeding-independent-review.md) verifies the stored identities, input histories and four neural-only replays, preserving the original failed feedback-equality control.

An isolated [proboscis geometry audit](proboscis-mechanics.md) verifies source mesh transforms and a two-pitch diagnostic Jacobian. The source has no NeuroMechFly oral-motion limits, extension trajectory or identified mouth aperture; generic hinge defaults and an arbitrary distal mesh vertex cannot supply them. No physical swallowing claim or runtime oral controller was added.

Latest complete suite recorded before the final habitat renderer adjustment: **175 tests passed, 54 subtests passed**, with 22 Brian2 dependency deprecation warnings, at `ef6e230`. The final renderer adjustment at `62129a3` passed its focused **11 tests / 26 subtests** and separate **26/26 scene checks**. Subsequent research-only work did not rerun the full suite. Further experimental modules remain subject to their own validation.

The [ten-second loop experiment](extended-loop.md) completes three full-graph conditions with finite, upright bodies and conserved resources. It retains one failed gate: after releasing motor mute, the fly requests feeding at the food patch rather than locomotion. Its [47-check independent review](extended-loop-independent-review.md) confirms that outcome and the actual mute release. No sampled wall interactions occur, and the current-based voltage failure remains. This is bounded engineering robustness evidence, not a completed natural-behavior milestone.

The [fixed posture-hold experiment](flybody-stance-experiment.md) now passes stopping and restart with source-native measured-length and neutral-zero position targets plus maximum adhesion. The last-commanded-target candidate fails on resumption, and the original policy still fails its stopping criterion. An [independent review](flybody-stance-independent-review.md) reconstructs all twelve traces, actuator transmissions and filters. This supports a bounded optional engineering adapter; it does not resolve the measured gait mismatch or establish neural stance control.

[Optional world illumination](world-illumination.md) adds a fixed scene lamp without changing sampled physical states at 21 times across 1,000 steps. Thirteen diagnostic checks and the live viewer light/reset inspection pass. The former unilluminated multiview result remains the record for the legacy configuration; visual-to-neural mapping remains open.

The [DoOR odor audit](door-odor-audit.md) now records chemical identities, missing and signed baseline-relative consensus evidence, selected source-study values, and candidate joins to all 2,635 named male ORNs. Updated olfactory identity data expose ambiguities and one palpal-label/nerve conflict. No dimensionless score is used as a firing rate, no missing response is filled with zero, and no new odor-driven behavior is claimed.

The [optional native FlyBody bridge](flybody-bridge.md) reproduces two original 2 s motor traces exactly. Four [actual full-graph trials](flybody-loop.md) complete with synchronized clocks, finite states and conserved resources. Their [independent journal review](flybody-loop-independent-review.md) checks all 2,500 ticks. Sensory-output blocking changes downstream activity under initially identical input, but blocking sweet output in the on-food pair leaves MN9 counts and intake unchanged. One frozen pre-run documentation digest is unrecovered; runtime, configuration and data-receipt hashes match the recorded source revision. The optional viewer now pauses normally at 2 s with working Reset and camera controls. Longer rolling-reference operation remains a separate experiment.

The [primary Or42a audit](or42a-primary-assay.md) distinguishes a measured male spontaneous rate from baseline-subtracted odor responses and separately identifies all 36 male VM7d sensory cells. Exact original DoOR offset preprocessing remains unresolved. Any proposed numerical rate replay must distinguish imposed excitation events from actual source spikes and must not treat liquid dilution as airborne concentration.

The [Or42a primary-summary excitation experiment](or42a-summary-experiment.md) completes all 15 fixed full-graph trials. Its [independent review](or42a-summary-independent-review.md) reconstructs 9.69 million ordered spikes, exact excitation RNG streams and delayed edge counts from the saved traces. Pulse-window source rates are close to the imposed summary rates, but requested events, applied increments and actual source spikes differ. Source-output blocking removes all downstream activity; the unblocked model retains implausible voltages and no walking-group pulse response. Brief startup turning commands remain explicitly unrealized because no body ran.

The [full-neural rolling extension](flybody-rolling-loop.md) now completes three fixed 12-second trials, with [independent review](flybody-rolling-loop-independent-review.md) of all 18,000 physical intervals, 31.49 million ordered spikes, exact original two-second prefixes and final checkpoints. Direct-probe conditions travel about 91/90 mm and stop/resume through three readout mutes. Sensory-only commands rest throughout and travels 0.015 mm; none contacts food or ingests it. The sampled minimum reaches −532.58 mV. [Endpoint attribution](flybody-rolling-loop-outcomes.md) identifies repeatedly extreme cells outside the actual sensory/probe input lists, so removing the locomotor probe alone does not resolve this voltage failure.

The [isolated ORN→PN audit](orn-pn-transfer-audit.md) and [independent matrix-exponential review](orn-pn-transfer-independent-review.md) distinguish contact-rule synaptic increments from passive voltage peaks. All 616 selected same-glomerulus ORN→adPN edges are individually subthreshold from rest in this fixed model. Female somatic recordings cannot directly set the male point-neuron gain; a measured temporal-transmission comparison is the next constraint.

The optional [FlyBody airflow observation](flybody-airflow-runtime.md) uses independently reviewed proximal-antenna positions, Jacobian velocities and an articulated head frame. Enabling it passes a fixed two-second full-neural prefix regression with original physical states, ordered inputs, RNG and spikes unchanged. It adds observable local flow, with no aerodynamic force or wind-to-neuron mapping. The rolling viewer without wind has verified pause/camera/reset beyond two seconds; the airflow viewer is available separately on port 8773.

## Compute and next gates

Active neural-only benchmarks took about 2.4–3.5 wall seconds per simulated second; full-loop checks about 5.9–7.6, excluding graph construction and renderer setup. Current RAM is sufficient. More CPU capacity can help parameter/seed sweeps. A GPU does not automatically accelerate the present Numba CPU implementation; hardware recommendations should follow a measured accelerated backend.

The later twelve-second FlyBody experiments took 170–177 wall seconds each for the instrumented loop, about 14.2–14.8 wall seconds per simulated second. These include full event journaling, lossless spike compression and checks under concurrent work; they are not isolated runtime speed benchmarks. The live viewer separately reported about 0.11× realtime during inspection. No additional compute is required for the current bounded diagnostic work.

The subsequent [two-cell recorded-input replay](negative-voltage-replay.md) reconstructs all six endpoint voltage/synaptic-state pairs bit-for-bit, including spike times and pending deliveries. Its [381-check independent review](negative-voltage-replay-independent-review.md) verifies 720,000 saved target-state transitions and classifies all 3,671,254 potential deliveries. It identifies dominant accepted inhibitory source types under each fixed recorded history. No full graph was rerun and no gain changed; this is numerical accounting, not a biological intervention. A separate [scalar depression reference](synaptic-depression-reference.md) passes an independent high-precision review, but its fixed-source predictions are not a fly physiology fit or an integrated neural mechanism.

Next, following the user's latest steering: the reviewed Eon neural panel is committed/pushed, the embodied benchmark is cancelled, and the [inhibitory C0/C1/H0/H1 factorial](inhibitory-factorial.md) is executed. Its numerical pass does not select H1: all four arms retain identical two-cell spike trains. Advance the [separate recurrent comparison](inhibitory-recurrent-design.md) with measured performance, paired seeds and recovery/input-off windows while retaining the [promotion gates](inhibitory-promotion-gates.md) for physiological amplitude/timing, meaningful contrasts and network stability. Physiological calibration, wind transduction, male visual registration and physical obstacle avoidance remain open. The paired-animal stage remains deferred.

The [VM2 recovery extraction](orn-pn-recovery-digitization.md) now retains 21 published markers, their drawn error extents and 400 printed-fit vertices, with 137 checks. These are graph-derived summaries, not original trials; protocol and normalization gaps remain explicit. The [inhibitory literature audit](../research/13-antennal-lobe-inhibitory-constraints.md) distinguishes postsynaptic fast/slow inhibition, presynaptic release suppression and spiking/graded source modes. It supplies no exact-target inhibitory gain or reversal. A separately [reviewed viewer fix](viewer-failure-independent-review.md) exposes cached failure clocks while labeling the last completed telemetry and image; normal advance behavior remains unchanged.

The subsequent [public-data inventory](salman-source-inventory.md) traverses the initially empty OSF root and its children: 54 public components and 258 file entries. Only two small Figure 3d analysis files were acquired separately. Their [current-clamp audit](salman-current-clamp-data.md) verifies all 8,434 formula caches but finds an incomplete cohort, an unexplained duplicated row and a factor-of-two discrepancy between the published figure's current axis and the methods/caption. The saved regression direction supports the prose; exact specimen joins and physical currents remain unresolved, so no neural calibration is adopted.

The [Wilson–Laurent supplement](wilson-gaba-reversal-source.md) is now recovered from the public author attachment. Its somatic GABA reversals depend on recording solution, and open markers denote unreached reversals. The approximately −75 mV holding limitation is not an identified physiological reversal. This closes an access gap without assigning an exact MaleCNS parameter.

The optional [FlyBody habitat](flybody-habitat.md) now adds a 30 × 24 mm physical enclosure and aligned planar food/water regions. Exact default and pre-contact state/actor parity pass. Straight walking contacts a wall at 0.666 s and reaches the unchanged reference-error termination at 0.768 s. Original detached-force checks remain 64/65 and shallow probes 29/31; the [460-check independent review](flybody-habitat-independent-review.md) preserves both failures. Final scene-removal checks pass 26/26 after retaining the first renderer's residual shadow defect. [Actual viewer controls and failure reporting](flybody-habitat-viewer.md) are checked separately; sensory-only movement remains absent.

The [expanded Eon repository audit](../research/15-eon-public-code-audit.md) now retains exact P9/leg protocols and author output tables. A read-only local comparison verifies 12 motor readout cells with optional grooming enabled and 8,276 incoming edges; it reconstructs the earlier taste trial's 0.586 mm net displacement and intake from saved results without rerunning the model. That taste trial also retained an implausible sampled minimum of −284.05 mV. The [ascending-leg crosswalk](eon-leg-afferent-crosswalk.md) distinguishes primary sensory afferents from two ascending interneurons and preserves the unresolved eight-to-two male correspondence. No runtime defaults or graph weights changed.

Parallel research recovered the [Nagel–Wilson inhibitory-transfer source](ln-inhibitory-transfer-source.md) and measured figure timing/layout, initially with no neural-trace extraction or fit. The [obstacle review](../research/14-obstacle-avoidance-evidence.md) identifies the male TLA pair and seven TLA→MDN edges containing 255 contacts, while physical touch transduction remains unresolved. The earlier [inhibitory factorial design](inhibitory-factorial-design-review.md) was subsequently frozen and executed as recorded below; its former Eon pause is superseded.

The first [Eon-inspired P9-context panel](eon-p9-context-assay.md) completes 12 fixed neural-only trials with the known P9 pair and eleven LgAG2 cells. All 167 consistency checks pass, with 606,759 ordered spikes saved. P9 recruits steering and taste modulates it, but both forward readout cells are silent from 50–500 ms in all P9-only and combined trials. The sampled minimum reaches −474.39 mV; seed-dependent steering and feeding responses remain. No body ran and no default changed. The omitted ascending pair, source graph, stimulus cohort, trial duration and trial count prevent calling this a literal source replication.

The [inhibitory factorial](inhibitory-factorial.md) completes all 12 arm/history trials with 137 checks and 40 separate synthetic event controls. Its [565-check independent review](inhibitory-factorial-independent-review.md) reconstructs all 2.88 million target intervals and 14.69 million event dispositions. C0 reproduces all original per-tick states, spikes and delivery accounting bit for bit. Hybrid minima remain above −75 mV with no clamp; 1,488 selected intervals agree with independent quadrature/ODE references. Current-only handling changes leave the extreme minima intact. Every arm retains the same spike trains: no spikes in 67052, and one early spike in 13314 for two histories. H1 retains extra inhibition and has a roughly 3.25 mV deeper startup minimum than H0; it is not promoted. No recurrent network or body ran.

The [BANC audit](../research/17-banc-comparative-circuit-insights.md) retains the official female CNS metadata, five exact lLN2T_b and four M_vPNml50 candidates, and existing motor-type matches. Neither exact male replay target has a released explicit counterpart in that snapshot. Historical labels, predicted/curated transmitter conflicts, versioned root IDs and unsigned influence metrics remain separate; no anatomical comparison supplies a fitted inhibitory current or time constant.

The [single Nagel–Wilson Figure 6 extraction](ln-inhibitory-transfer-phase2.md) now resolves late-minus-early current at **+1.489 to +2.292 pA**, while the separate presynaptic cohort's rate changes by **−15.657 to −8.608 spikes/s**. All 53 extraction-validation checks pass. These outward-rounded intervals enclose graphical mean ink and calibration uncertainty, not SEM or confidence intervals. D/F are the same current cohort; the rate SEM and overlapping control mean remain unresolved. A PDF-origin error in the first overlay was corrected separately, with the original retained and numerical extraction unchanged. No model fit or conductance conversion was made.

The [independent P9 results review](eon-p9-context-independent-review.md) passes 1,483 saved-data checks, confirms all readout windows and reconstructs every uniform draw with separate integer arithmetic. Taste-output blocking restores the entire non-taste spike stream and final states exactly to P9-only. A raw checkpoint contains −423.15 mV, independently confirming the physiological failure. The initially overbroad reviewer assertion is retained with its correction; no experiment data changed. No embodied benchmark was run after the user's cancellation.

The [recurrent performance probe](inhibitory-recurrent-performance.md) now completes five 50 ms full-graph runs with 280 checks. C0's complete endpoint state matches a fresh original-engine reference byte for byte, with matching 5 ms state digests and retained old spike/input prefixes. Spike counts are 113 for C0, 2,861 for C1, 99 for H0 and 150 for H1. Hybrid phase minima remain above −75 mV; current-based minima remain implausible. This establishes short recurrent execution and exposes growing integration cost, not physiological fidelity or stability after withdrawal. The initial scalar timing-helper compilation failure is retained separately; no graph ran in that failed attempt. Production and viewer defaults remain unchanged.

The [conditional 5 ms filter test](ln-inhibitory-linear-filter-results.md) finishes one frozen evaluation with 16 checks, followed by [46 independent review checks](ln-inhibitory-linear-filter-independent-review.md). Its guarded unit-gain late-minus-early enclosure is negative throughout, conservatively displayed as [−16.401354, −8.649020], while the published current difference is positive. Every positive fixed gain and finite nonnegative initial filter state preserves that incompatibility. The result concerns direct mapping of the separately recorded, smoothed graphical means; it does not reject biological receptors or recurrent network explanations and supplies no fitted conductance.

The [four-thread recurrent implementation](inhibitory-recurrent-parallel-performance.md) now preserves all retained numerical outputs in the matched 50 ms full-graph probe, with 1,242 checks passing after 103 synthetic checks and a 176-check independent implementation review. H0/H1 kernel time falls by observed factors of 1.72/1.65 in these separate runs; unchanged serial C paths also vary, so this is not a timing-confidence or long-run forecast. The method parallelizes independent interval calculations and commits events in the original order, without changing model equations or runtime defaults. That short probe did not cover the full stimulus/withdrawal panel.

The [full recurrent panel runner](inhibitory-recurrent-panel-execution.md) is implemented with durable chunk/checkpoint archives, selected numerical-reference intervals, streaming complete-window metrics, and isolated workers. All seven preflight components pass (666 checks), and the plan, source, input streams and environment are frozen and rechecked before the first full-panel execution. No runtime model or H1 promotion follows from these engineering checks.

The [frozen full panel](inhibitory-recurrent-panel-execution.md) is now running after source/plan commit `91b6820`. All five C0/seed-11 controls complete three seconds and reproduce their original 1.5-second prefixes exactly, totaling 7,000,974 saved spikes. Independent quiet/active-state and count audits confirm the first baseline's continued nonsource activity after withdrawal: roughly 5.25–5.29 Hz per cell across all three off bins, with no source spikes and silent walking readouts. The bins show no monotonic increase; long-term stability and physiology are unresolved. The other C0 seeds are in progress, and altered arms await all fifteen control gates. See the [fixed first-seed snapshot](../validation/inhibitory-recurrent-panel-first-seed-snapshot.json); this is not a completed panel or H1 promotion.

The [complete C0 control audit](inhibitory-recurrent-panel-c0-results.md) verifies all fifteen trials and 21,546,618 spikes; its fifteen-prefix gate preceded all altered arms. EA-minus-constant first-hop responses remain +5.825/+6.647/+6.471 Hz per cell across seeds, while the population-wide nonsource contrast changes sign. Nonsource off rates stay at 5.2353–5.3529 Hz per cell across all unblocked conditions. All C0 forward and fixed replay-target spike counts remain zero. An [independent scalar implementation](inhibitory-panel-reference-audit.md) passes 125 frozen cases. These intermediate results are now superseded for cross-arm interpretation by the [full-panel report](inhibitory-recurrent-panel-results.md).

The completed panel retains 157,832,582 spikes with 2,871,695 component saved-data checks; all 12,564 producer contrasts are independently recomputed. Final-integrity review verifies all 109,952 manifest-listed files, and combined reduction passes 554,896 checks. C1/H1 handling raises constant-input late-off nonsource activity by about 9.7–10.0 Hz/cell against its corresponding original-handling arm; voltage-dependent inhibition lowers it by about 2.0–2.2 Hz/cell. First-hop EA/IA contrasts persist in all seeds, while the forward pair remains silent. No candidate is selected. The suggested decay/reception/retention decomposition has not been run and is superseded by the user's navigation-ladder direction. Physiological calibration, natural sensory behavior and the broader single-male milestones remain open.

The [navigation anatomy audit](../research/18-navigation-ladder-anatomy.md) now resolves the requested cohorts and verifies the quoted route contacts against original rows, retaining exact-type/family and strict-cognate distinctions. It selects 1,314 annotated DNs, including ten midline cells; column angles and mirror identities remain unassigned. The [primary assay audit](../research/19-navigation-ladder-assay-evidence.md) separates near-source still-air behavior, wind-guided behavior, sex/state preparation and the corrected hDelta identities. [Observational metrics](navigation-ladder-metrics.md) add explicit spike exposure, kinematics, physical upwind projection and coordinate-dependent circular summaries; 25 focused analytic tests pass. The [six-trial H1 timing reduction](navigation-ladder-timing.md) retains exact selected spikes and confirms sustained PN recruitment. Independent recount and exact-sequence comparison show identical MBON12/13/14 output trains between matched EA/control conditions in all three seeds. These measurements do not change runtime encoders or the motor decoder, or identify a unique mechanism.

The [VM2 depression-panel extraction](orn-pn-depression-digitization.md) resolves 40 noninitial means and 37 error spans with independent raster review. A [conditional comparison](orn-pn-depression-comparison.md) against the previously frozen scalar reference yields RMSE 3.01/14.76/30.60 percentage points at 15/20/50 Hz, versus 24.79/47.34/93.99 for a static null. The fixed reference retains too much high-frequency amplitude. Exact biological normalization and phase remain unresolved, and the data are female VM2; no male transfer or runtime mechanism is promoted.

The subsequent [bounded calibration](orn-pn-depression-calibration.md) fits the nine noninitial 20 Hz means only, with 270 manufactured preflight cases and 689 independent review checks. It reaches the 100 s recovery ceiling. A separate [shape-boundary analysis](orn-pn-depression-shape-boundary.md), with 95 preflight cases and 274 independent checks, detects a compatible finite inverse at retention 0.8082895 and recovery 103.8654 s. Development RMSE improves only 0.00306 percentage points to 4.46290; fixed-parameter evaluation RMSE is 12.38813/21.17165 at 15/50 Hz. The implied first-test efficacy is only 0.0095944 of the declared fully recovered model scale, which the normalized data do not measure. A [source-history check](orn-pn-depression-conditioning-history.md) finds no explicit full-recovery criterion or Fig. 8-specific inter-train interval. This is a negative calibration result with retained artifacts, not a physiological parameter estimate. Runtime source, neural parameters, sensory encoder, decoder and body remain unchanged. The next bounded saved-data audit concerns presynaptic inputs to saturated MBONs; no new neural batch has started.

The completed [MBON audit](navigation-ladder-mbon-inputs.md) covers 9,484 incoming edges and 4,722 sources for ten MBON12–14 cells. Producer checks pass 37,180 checks; independent raw-archive review passes 178,371 and matches every reconstructed `p/h` row plus all 48 checkpoints. Every MBON ISI from 0.5 s onward is at the 22-tick model limit in all six trials. The selected 8,236 positive Kenyon-cell edges satisfy the prospective class rule. The isolated intervention extension then passes 157 synthetic checks and 3,580 actual-graph inactive-prefix checks across all six conditions, with unchanged full dynamical endpoints and queues. These are implementation and conditional input-state results; the active intervention outcome and independent numerical reference review remain pending.
