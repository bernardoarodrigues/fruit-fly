# One KC-to-MBON delivery intervention

Status: the [six-trial execution plan](../validation/navigation-mbon-intervention-plan.json) completed in `runs/20260905T185111580463Z-navigation-mbon-intervention`. All independent state/delivery, numerical and combined outcome reviews pass. The [combined results](navigation-mbon-intervention-results.md) show a large reversible effect without physiological repair: MBON12/14 become silent, MBON13 retains high firing, odor contrast remains negligible, and saturation returns after restoration. The quiet completion heartbeat is paused. The [launch receipt](../validation/navigation-mbon-intervention-launch.json) remains the historical launch record after source/plan commit `db9ae70`.

## Intervention and controls

Use all three existing seeds (11, 12, 13), with ethyl-acetate and constant-baseline conditions. Start each branch from its exact full H1 checkpoint at tick 5000, including pending delayed spikes and the original external random stream. The original prefix is reused and linked as evidence, not claimed as a new resimulation. The six completed original trials remain controls.

Suppress only the 8,236 selected positive Kenyon-cell-to-MBON12–14 edge deliveries during ticks `[5000,15000)`. The condition uses arrival time rather than source spike time. It does not clear accumulated `p/h`, erase queued source spikes, block other projections or modify inhibition. Continue through tick 30000, so the final 1.5 s follows restoration of the selected deliveries with external input off.

The ten MBONs are added to the original 48 continuously recorded cells after state restoration. All 166,700 neurons continue to evolve; the new selection changes observation only. Save full-population window counts, exact emitted spikes, continuous selected voltage/p/h, suppression events, numerical reference candidates and full checkpoints.

## Implementation gates

The isolated [extension](../scripts/navigation_mbon_intervention_kernel.py) derives its functions from hash-pinned original source through seven exact single-occurrence patches. The new Numba dispatcher disables caching; it does not replace original functions or alter simulator/viewer defaults. Source-output blocking retains its original meaning, with separate suppression counters and disposition.

The [synthetic checks](../validation/navigation-mbon-intervention-kernel-checks.json) pass 157 checks for delivery boundaries, original/derived inactive parity, source masks, unaffected projections, checkpoint restoration, recording expansion and failure handling. The [development record](../validation/navigation-mbon-intervention-development/development-receipt.json) retains an initial checker assertion that incorrectly expected exact subtraction of separately accumulated floating-point states; corrected checks compare the ordered branch recurrences directly. No real experiment data were generated by those checks.

The [frozen live inactive probe](../validation/navigation-mbon-intervention-prefix-plan.json) then checks all six actual graph conditions for ticks 5000–5050 with a nonempty edge mask whose window is inactive. Its [result](../validation/navigation-mbon-intervention-prefix-results.json) passes 3,580 checks: historical kernel outputs, the original 48 projected trace columns, full endpoint state/queues and newly recorded MBON trace endpoints agree. This is 5 ms of implementation parity, not an active intervention result or physiological validation.

## Completion and interpretation

The [runner](../scripts/experiment_navigation_mbon_intervention.py) uses isolated sequential workers, immutable plan/input hashes, complete chunk/checkpoint archives, parent/worker time limits and failure retention. A returned `passed` terminal means the fixed batch completed its online invariants. Independent reference calculations and saved-data review remain required before scientific acceptance.

After all six trials finish, verify complete coverage, source/queue/spike counts, every suppression identity and window, and all numerical reference intervals under the original panel tolerances. This includes adaptive quadrature, independent ODE and order-64 comparisons for saved global candidates and each declared target's first available interval per chunk. Retain threshold-margin ambiguity rather than declaring an accurate spike from a merely finite voltage.

Compare each target's spike counts and 22-tick ISI occupancy, paired EA-minus-constant pulse contrast, voltage/p/h response, and local versus population-wide off activity against the saved controls. Inspect restoration after 1.5 s. Analyze the complete batch together after a quiet completion trigger; do not alter the intervention based on an early trial. Failure stops the batch and preserves evidence without automatic restart.

Reduced MBON saturation would support a contribution of this pathway under the model's current assumptions. Restored odor contrast, persistence, physiological amplitudes and navigation are separate questions. No H1 promotion, new depression parameter, body integration, decoder change or natural behavior claim follows automatically.
