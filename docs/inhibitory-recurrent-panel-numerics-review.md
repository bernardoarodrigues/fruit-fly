# Panel worker numerical and protocol review

The worker's numerical/protocol source review found no remaining blocker after three reference-collection corrections. The bounded [review script](../scripts/review_inhibitory_recurrent_panel_numerics.py) passes **30/30 checks**, with [its receipt](../validation/inhibitory-recurrent-panel-numerics-review.json) and [exact dispatch fixtures/results](../validation/inhibitory-recurrent-panel-numerics-review-cases.json) retained. No network, body or full-graph trial was executed. The checks call mocked reference methods and, once, the real reference methods on a previously saved scalar input.

The reviewed [runner](../scripts/experiment_inhibitory_recurrent_panel.py) is pinned in the receipt. C0 uses the same 36 ordered inputs and externally retained RNG stream as the earlier Or42a protocol. Each 5 ms prefix checks ordered spikes, candidate and applied masks, RNG boundaries, actual edge visits and voltage extrema. At 1.5 seconds, its endpoint voltage/synaptic arrays and all spike records must match. Summing startup and baseline count rows reconstructs the earlier first 500 ms window; the next two rows match its stimulus and recovery windows. Both old and new count arrays use int64. This does not assert an unavailable intermediate full-state checkpoint from the earlier experiment.

All arms use their newly generated spikes for refractory and pending-queue checks. Stable sorting by neuron retains time order within each cell; the first spike is compared with that cell's previous last spike. Queued spikes retain arrivals at or beyond the completed boundary, using an 18-tick delay and 19 slots. Source-output blocking is evaluated at delivery, while positive direct events retain the source's same-tick-spike rejection rule. Zero-rate intervals still share the listed-input and external-stream conventions. No-input and blocked-source controls check their declared quiet/nonsource behavior. Fixed event audit ranges align with chunk boundaries and change edge logging, not the selected trace population.

The reference collector now uses each target's first available row within the chunk, rather than skipping a target refractory at row zero. It merges every selection reason when global rankings and target sampling identify the same `(tick,index)`. Global input states come from the kernel's retained winning intervals; target inputs come from the corresponding trace boundary and availability row. The collector checks production against 64-point quadrature and adaptive quadrature, and compares the independent ODE result with the latter under the frozen tolerances.

A method exception now retains its exact interval, input state, method in progress and error. Earlier method values and completed reference rows survive. Later selected rows are explicitly unattempted after a failure. The chunk is archived before its failed accuracy gate stops the worker. Mocked adaptive-quadrature, ODE and 64-point exceptions, an interruption and an ordinary accuracy failure exercise these paths. The real saved scalar input passes the actual three-method dispatch and reproduces its frozen 64-point result.

These numerical checks sample completed-prefix intervals. Failed-tick kernel inputs and partial reference candidates remain raw failure evidence; they are not passed numerical reference comparisons. A threshold margin within the observed numerical error is recorded as unresolved timing robustness, consistent with the design. Finite voltage, a lower-bound pass and this bounded review do not validate physiology or select H1. Publication/resource/parent-child interruption handling and the aggregate input/metrics audit have separate reviewers; this report is not a substitute for their checks or for the actual recurrent panel.
