# Independent source review of the inhibitory factorial driver

**No source/event/arm correctness blocker found in the inspected driver.** This is a pre-execution source review, not validation of produced factorial data. No replay, neural network or body was run by this reviewer. Driver SHA-256 at this inspection: `1c9fd17d2c9a4c46cf223c119828896af26e80b580ae15dc6517ca2f1fd0a385`. The source was still being completed before plan freeze; later source changes need review or an addendum.

The review compared [the driver](../scripts/inhibitory_factorial_replay.py), [the interval solver](../scripts/inhibitory_factorial_solver.py), [the prospective design](inhibitory-factorial-design-review.md) and the existing signed-current engine. The prior [input audit](../validation/inhibitory-factorial-input-audit.json) reports 258 passing checks for identities, ordered events, weight bytes, source masks and baseline bookkeeping. This review inspected that receipt; it did not independently repeat all 258 checks.

The four arms implement the intended two factors. C0 retains one signed synaptic accumulator and the original floating-point expression. C1 changes the whole handling package: decay and accept unblocked input through refractory intervals and firing ticks, then reset voltage only. H0/H1 replace only negative-weight accumulation with the declared inhibitory leak ratio, retain positive current input and apply the corresponding handling package. Every arm determines its own availability, threshold crossings and reset times. The source histories—including spikes from either target—remain exogenous and never use a newly generated target spike.

The event inventory expands ordered recorded source events, includes zero-weight edges and delays by 18 ticks. Its source/target mapping is unique. Each in-horizon event is assigned blocked, target-unavailable or accepted, while beyond-horizon events remain pending before any future mask decision. Blocking is applied at delivery. Saving threshold voltage before reset and the resulting state at index tick+1 preserves the original clock distinction. Global ordering between the two independent target slots does not create interaction between their states.

The outer arm loop runs all three C0 histories before any altered recorded arm. C0 gates include full voltage/signed-state traces, emitted spikes, ordered accepted ticks/edges, signed jumps, all per-edge dispositions and endpoint state/mask checks. The source provenance check retains absence of direct target drive. A baseline failure stops progression.

The scalar solver has separate frozen synthetic evidence: 125 cases, 1,282 checks, exact excitation-only source arithmetic and two independent numerical references. The driver's newly added synthetic controls test firing at tick19 after a tick18 impulse, unavailability through tick40, release at41, explicit delivery eligibility, firing reset/retention, blocked and pending precedence, no-input rest and excitation-only C/H equality. Its moderate23mV impulse is correctly aligned: state[19] is elapsed0 after delivery, with46 samples through4.5ms. The impulse remains subthreshold. These controls are written but were not executed by this reviewer.

For H arms, the driver compares32/64-point integration from the same preceding state on every available step. Adaptive quadrature, a second ODE solver and internal subdivisions use predeclared selected steps from each arm's own state. Thresholds remain on the original0.1ms macro clock. The checks distinguish the mathematical lower bound from an invalid universal upper bound of0mV. They remain numerical checks, not biological calibration.

Two evidence qualifications were sent during the initial source review and have now been addressed in the final version below:

- The initial version could lose the current arm's partial arrays when the compiled interval helper raised. Prefix retention or a narrowed promise was requested. Completed traces that subsequently failed a numerical gate were already saved before the gate raised.
- An explicit check of H1 inhibitory-state decay plus the next refractory arrival was requested in addition to the positive/signed-state check. Source inspection already found the update correct; this was a synthetic coverage request.

The final numerical outcome remains unknown. These are two isolated cells conditioned on three histories from one original run, with an engineering inhibitory reversal and fixed source masks whose time-resolved provenance is limited. Synaptic handling changes three mechanisms together. Favorable bounds, spike counts or voltage contrasts cannot establish recurrent stability, receptor physiology, behavior, or a default-model promotion.

## Final preflight addendum

Reviewed final driver SHA-256: `05342f151a45a18235824c931633f627a6d9211b07b9fd37ae2bc0c4733ebbcb` (460 lines). **Both requested changes are addressed at source level; no preflight blocker remains.** The driver now catches H interval exceptions and returns every completed macro-step plus the failing input state and its tick/target. Slicing through `state[:tick+1]` excludes both targets' partial failing step; unprocessed events retain disposition4 rather than being labelled accepted or beyond-horizon pending. The outer driver saves this prefix archive before raising and identifies it in the exception message. Process termination/allocation failures are explicitly outside the prefix-retention guarantee.

The synthetic suite now includes an intentionally invalid infinite-input case expected to fail at tick19/target0, with a20-state prefix and19 completed-step records, and verifies remaining unprocessed dispositions. That invalid artifact is labelled separately. The H1 refractory inhibitory state is explicitly checked as prior h times the decay factor plus1. Preparation also requires the existing solver and input-audit receipts to have passed. These additions were inspected, not executed by this reviewer; compilation, synthetic execution, all three C0 reproduction gates and recorded-arm numerical checks remain required.
