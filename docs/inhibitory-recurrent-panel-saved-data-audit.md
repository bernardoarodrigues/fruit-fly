# Saved-data audit of the recurrent panel

The first bounded audit **passed 35,811/35,811 checks on its first execution**, covering the completed C0, seed 11, no-input trial from frozen plan `c7af957eb89a04a3edbbcc2a9d9b2b2d6936a07c4b3fe019448287467e9183d5`, run `runs/20260905T060501089987Z-inhibitory-recurrent-panel`. The production source/plan commit is `91b6820`. The [independent receipt](../validation/inhibitory-recurrent-panel-00-C0-seed11-no_input-independent-review.json) records no failures. The separate [reader](../scripts/review_inhibitory_recurrent_panel_trial.py) waited for the authoritative complete `terminal.json`, then decoded 611 archives and 19,319 arrays in 2.62 seconds. It imports no producer, neural kernel or simulator.

All 600 chunks cover exactly 30,000 ticks / 3 seconds, and all eight full boundary checkpoints match the initial native arrays. The 166,700 cells remain at −52 mV with zero synaptic states, spikes, candidate/applied arrivals and delivered edges. Counts reconstructed from raw spike records match the retained count matrix and complete-window summaries. Selected traces, global phases, histogram bins, RNG boundaries and the durable old C0 prefix gate agree. The worker's terminal reports completion in 68.47 seconds; that timing is a producer measurement, separate from the saved-data audit.

The separate [first active baseline audit](inhibitory-recurrent-panel-baseline-independent-review.md) is also complete: 38,229 checks pass, including all 48 selected voltage/synaptic transitions, exact delayed selected-edge records, raw counts and checkpoint queues. It confirms sustained nonsource activity throughout that trial's input-off interval. Its new reader and receipt preserve the original quiet-control evidence.

The reader independently decodes typed JSON and NPZ payloads, verifies completion transactions and payload/array checksums, and restores packed spike fields to their original integer dtypes. It refuses incomplete-publication evidence. It then walks chunks in exact order through 30,000 ticks, reconstructs window counts and the last-spike/pending-queue history, verifies the fixed 48-cell traces across boundaries, and compares all full boundary checkpoints. For the no-input control, it additionally checks resting voltage, zero synaptic states/spikes/arrivals/edges, eligibility masks, complete global phase diagnostics, work counts, histogram bins and cohort summaries. The durable C0 prefix gate is checked against its actual old trace and checkpoint marker; its assertions alone are insufficient.

The first actual audit execution is preserved. Its receipt SHA256 is `09e05b82705cb275225e9a90583938a3375b96306c75cd015f2bbe5068adbc2f`; reader SHA256 is `1e2d91757813372de3fa3d5df100690ab7ac6d3805fe7f7035e3ebc5f6424da8`. Running `.venv/bin/python scripts/review_inhibitory_recurrent_panel_trial.py --ordinal 0` writes a separate review receipt only after a completed terminal exists and refuses to replace an earlier audit. A read-only decoder check on the retained tiny archive fixture passed 20 checks before the panel audit, including endian spike recovery and NaN/Inf retention. Neither check ran a network.

For subsequent completed trials, the same archive/clock/count/checkpoint foundation can be reused. Activity requires additional comparisons: reconstruct applied direct events from the shared candidates and actual source firing ticks; derive selected availability and reset masks from each arm's own spike history; verify selected endpoint voltages from retained prethreshold values, direct increments and reset decisions; and check incoming event logs against source spikes, delayed CSR edges, fixed source blocks and target eligibility. Event-log population varies only at the predeclared audit ranges, while all 48 traces remain present. Counts must be accumulated exactly once from completed-prefix spikes, excluding any separate partial-tick stream.

H review must join each saved numerical reference to its captured global interval or its target's exact selected before/after trace row. Recompute its error arithmetic, threshold-margin flag and frozen accuracy gates; independently re-evaluate retained scalar states with quadrature/time-domain references under a declared bounded audit scope. No source spikes may be regenerated. Captured global winners may lie outside the 48 traced cells: their stored states and source selection code can be checked, but their global optimality cannot be independently reconstructed from a missing all-cell time cube. That limit must remain distinct from the numerical accuracy of the saved interval itself.

The authoritative terminal status controls eligibility. A correctness, interruption or retention failure is excluded from scientific contrasts even if it left many complete raw chunks. A resource-limited trial may contribute only explicitly complete, correctly retained windows. Keep `confirmed_prefix_end_tick`, `last_durable_chunk_end_tick`, `counts_end_tick`, and `last_complete_checkpoint_tick` separate on failures. Never treat an incomplete/absent window as a zero response or a mutable partial checkpoint as a coherent completed state. A passing no-input audit certifies only that control; it does not establish active-network physiology, persistence control, seed robustness or H1 promotion.
